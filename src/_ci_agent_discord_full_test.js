// Temporary file to trigger a real CI failure to confirm the full
// assessment (not just a link) renders correctly in Discord. Safe to
// delete after viewing.
const unusedDiscordFullTestVariable = 'this failure is intentional'

export function discordFullAssessmentTest() {
  return 'ok'
}
