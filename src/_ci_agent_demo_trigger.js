// Temporary file to trigger a real CI failure so ai-ci-agent can be
// demonstrated posting a real assessment. Safe to delete after viewing.
const unusedDemoVariable = 'this failure is intentional'

export function ciAgentDemoTrigger() {
  return 'ok'
}
