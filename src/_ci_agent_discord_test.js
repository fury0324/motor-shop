// Temporary file to trigger a real CI failure to confirm the new
// Discord notification step posts to the channel. Safe to delete after
// viewing.
const unusedDiscordTestVariable = 'this failure is intentional'

export function discordNotificationTest() {
  return 'ok'
}
